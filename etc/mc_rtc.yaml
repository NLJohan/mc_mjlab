MainRobot: JVRC1
Timestep: 0.002

# mc_rtc's built-in posture-holding controller.
Enabled: ismpc_walking

# disable GUIServer is needed to be able to run several controller concurrently
GUIServer:
  Enable: False

# Disable mc_rtc's per-controller binary log files when lanching a lot of envs
Log: False
